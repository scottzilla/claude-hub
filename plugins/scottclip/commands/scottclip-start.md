---
description: Start the ScottClip MCP server (webhook + MCP tools + OAuth)
argument-hint: "[--stop]"
---

Use the Skill tool to invoke skill: "scottclip:scottclip-start", args: "$ARGUMENTS"

Do not attempt to follow the skill instructions directly. You MUST call the Skill tool to load and execute the skill.

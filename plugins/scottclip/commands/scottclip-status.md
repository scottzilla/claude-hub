---
description: Show ScottClip status (queue, blocked issues, last heartbeat)
argument-hint: "[--history]"
---

Use the Skill tool to invoke skill: "scottclip:scottclip-status", args: "$ARGUMENTS"

Do not attempt to follow the skill instructions directly. You MUST call the Skill tool to load and execute the skill.
